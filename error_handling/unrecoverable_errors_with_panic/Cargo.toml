[package]
name = "unrecoverable_errors_with_panic"
version = "0.1.0"
edition = "2024"

[dependencies]

# We can set panic to 'abort' if we dont want rust to walk back up and 
#clean the stack after a panic occurs

# [profile.release]
# panic = 'abort'
